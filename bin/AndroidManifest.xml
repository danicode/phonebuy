<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phonebuy"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:icon="@drawable/ic_phonebuy_logo"
        android:label="@string/app_name"
        android:theme="@style/PhonebuyTheme" >
        <activity
            android:name=".view.initial.FrmInitial"
            android:label="@string/title_app" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.scan_barcode.FrmScanBarcode"></activity>
        <activity android:name=".view.about.FrmAbout"></activity>
    </application>

</manifest>